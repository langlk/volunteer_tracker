<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css'>
    <title>Volunteer Tracker</title>
  </head>
  <body>
    <div class="jumbotron">
      <div class="container">
        <h1>Volunteer Tracker</h1>
        <ul class="nav nav-pills">
          <li role="presentation" class="<%= @section == 'home' ? 'active' : nil %>">
            <a href="/">Home</a>
          </li>
          <li role="presentation" class="<%= @section == 'projects' ? 'active' : nil %>">
            <a href="/projects/all">Projects</a>
          </li>
          <li role="presentation" class="<%= @section == 'volunteers' ? 'active' : nil %>"><a href="/volunteers/all">Volunteers</a></li>
        </ul>
      </div>
    </div>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
